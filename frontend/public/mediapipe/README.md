# MediaPipe WebAssembly runtime

These six runtime files are copied from `@mediapipe/tasks-vision@1.0.1/wasm` so the application does not depend on a runtime CDN. The npm package identifies its license as Apache-2.0.
